--- original/./cloned_repos\click\examples\completion\completion.py+++ fixed/./cloned_repos\click\examples\completion\completion.py@@ -1,6 +1,8 @@-    click.echo("\n".join(os.listdir(dir)))
+```python
+import click
+import os
 
-
+@click.echo("\n".join(os.listdir('.')))
 def get_env_vars(ctx, param, incomplete):
     # Returning a list of values is a shortcut to returning a list of
     # CompletionItem(value).
@@ -10,12 +12,14 @@ @cli.command(help="A command to print environment variables")
 @click.argument("envvar", shell_complete=get_env_vars)
 def show_env(envvar):
-    click.echo(f"Environment variable: {envvar}")
-    click.echo(f"Value: {os.environ[envvar]}")
+    if envvar in os.environ:
+        click.echo(f"Environment variable: {envvar}")
+        click.echo(f"Value: {os.environ[envvar]}")
+    else:
+        click.echo(f"Environment variable: {envvar} not found")
 
 
 @cli.group(help="A group that holds a subcommand")
 def group():
     pass
-
-
+```